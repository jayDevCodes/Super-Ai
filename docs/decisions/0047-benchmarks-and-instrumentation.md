# Decision 0047 — Benchmarks and instrumentation

Status: accepted

Registry and sandbox controls expose deterministic benchmark harnesses and lightweight
thread-safe counters. Metrics contain counts and evidence fingerprints only; prompts,
secrets, payloads, and credentials are excluded.
